import HeroText from "@/components/ui/HeroText";
import Lists from "@/components/BucketList/Lists";

const Pangarap = () => {
  return (
    <section className="relative top-[20vh]">
      <HeroText
        title="pangarap"
        subtitle="the dreams we whisper, and the places we hope to find together"
      />
      <Lists />
    </section>
  );
};

export default Pangarap;
